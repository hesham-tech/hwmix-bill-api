# تقرير مراجعة عدم التطابق مع دستور المعمارية المالية (FAC-001)

يحتوي هذا التقرير على حصر دقيق للمواضع البرمجية والمنطق المحاسبي داخل مشروع **HWNix ERP** (الباك إند) التي لا تتوافق مع بنود **دستور المعمارية المالية (FAC-001)**، مع توضيح التأثير والحل البرمجي المقترح لكل منها.

---

## 📌 ملخص الفحص والامتثال

| البند في الدستور | الموضع البرمجي | حالة المطابقة | التأثير والمخاطر |
| :--- | :--- | :--- | :--- |
| **النواة المالية الحاكمة ورصيد الخزن** | موديول المصاريف (`Expense`) والإيرادات (`Revenue`) | 🔴 **غير مطبق** | يتم تسجيل القيود محاسبياً ولكن رصيد الخزنة في قاعدة البيانات لا يتغير نهائياً (خلل محاسبي حاد). |
| **جميع الأرصدة متطابقة مع Ledger** | تسوية ومطابقة الخزن (`CashReconciliation`) | 🔴 **غير مطبق** | عند اعتماد التسوية لا يتم ترحيل الفروقات وعجز/زيادة النقدية لتصحيح رصيد الخزنة. |
| ** Database Transactions والقفص الحصري** | دالة التحويل (`transferTo`) في ترايت `ManagesBalance` | ⚠️ **مخاطر Concurrency** | يتم خصم وزيادة رصيد الخزن دون استخدام `lockForUpdate()` مما يعرض الرصيد لمخاطر Race Conditions. |
| **منع التكرار والـ Dead Code** | خدمة ديون الموظف لنفسه (`UserSelfDebtService`) | 🔴 **غير مطبق / كود ميت** | كود معلق وغير مستدعى، ويقوم بكتابة سجل حركة وهمي دون تحديث الأرصدة الفعلية بالخلفية. |
| **عدم تخزين المتبقي (remaining_amount)** | موديل الفواتير (`Invoice`) وجدول قاعدة البيانات | ⚠️ **مخالفة معمارية مخزنة** | يتم تخزين وتعديل الحقل `remaining_amount` برمجياً بالجدول بدلاً من احتسابه ديناميكياً في الذاكرة. |

---

## 🔍 التفاصيل الفنية لنقاط عدم التطابق

### 1) الخلل الحاد في موديول المصاريف والإيرادات (عدم التأثير على رصيد الخزنة)
* **المشكلة البرمجية**: 
  * في [`ExpenseController::store`](file:///d:/Dev/projects/hwnix-bill-api/Modules/Accounting/Http/Controllers/ExpenseController.php#L39-L50)، يتم إنشاء المصروف مباشرة: `Expense::create($request->all())`.
  * يقوم [`ExpenseObserver`](file:///d:/Dev/projects/hwnix-bill-api/app/Observers/ExpenseObserver.php#L12-L16) باستدعاء `FinancialLedgerService` لتسجيل القيد الدفتري فقط.
  * **النتيجة**: لا يتم استدعاء دالة السحب `withdraw()` من الخزنة المحددة. رصيد الخزنة الفعلي في قاعدة البيانات (`cash_boxes.balance`) لا يقل بقيمة المصروف، مما يخلق تباعداً كلياً بين الأستاذ العام والرصيد الفعلي للخزن.
  * نفس الخلل يتكرر في موديول الإيداعات والإيرادات العشوائية (`Revenue`)؛ حيث لا يتم استدعاء `deposit()` لشحن رصيد الخزنة بالمال.
* **البند المخالف**: *"جميع الأرصدة متطابقة دائماً مع Ledger"* و *"الرصيد: لا يتم تعديل أي رصيد إلا من خلال Financial Engine"*.
* **الحل المقترح**:
  تعديل `ExpenseObserver` و `RevenueObserver` لاستدعاء المحرك المالي لخصم/إيداع المبالغ في الخزنة تلقائياً عند الإنشاء وعكسها عند الإلغاء/الحذف.

---

### 2) تجميد اعتماد التسويات المالية دون معالجة الفروقات في الخزن
* **المشكلة البرمجية**:
  * في [`CashReconciliationController::approve`](file:///d:/Dev/projects/hwnix-bill-api/Modules/Accounting/Http/Controllers/CashReconciliationController.php#L107-L137)، يقتصر اعتماد تسوية الخزنة على تغيير العمود `status = approved`.
  * **النتيجة**: إذا كشفت المطابقة عجزاً نقديّاً (مثلاً: -500 ج)، لا يقوم النظام بإنشاء حركة سحب تسوية لخصم الـ 500 ج من رصيد الخزنة الفعلي بالجدول لتتطابق مع الجرد الفعلي، مما يجعل اعتماد المطابقة مجرد عملية نصية بلا أثر مالي حقيقي.
* **البند المخالف**: *"جميع الأرصدة متطابقة دائماً مع Ledger"* و *"الخزائن والذمم والعمليات العكسية من مسؤولية Financial Engine"*.
* **الحل المقترح**:
  عند اعتماد المطابقة بنجاح، وإذا كان هناك فرق (`difference != 0`)، يجب أن يقوم النظام تلقائياً بإنشاء حركة تسوية مالية (إيداع فروقات تسوية أو سحب عجز تسوية) لتعديل رصيد الخزنة برمجياً لتتطابق مع الجرد الفعلي.

---

### 3) غياب قفل الحماية المتزامن في التحويلات المالية (`transferTo`)
* **المشكلة البرمجية**:
  * في ترايت [`ManagesBalance::transferTo`](file:///d:/Dev/projects/hwnix-bill-api/app/Traits/ManagesBalance.php#L312-L330)، يتم جلب الخزنة المصدر والهدف عبر `findOrFail` العادية ثم تطبيق `decrement()` و `increment()` مباشرة.
  * **النتيجة**: عند حدوث تحويلات متزامنة فائقة من أو إلى نفس الخزنة المشتركة في نفس اللحظة، سيتم قراءة رصيد قديم وتخزينه كـ `balance_before` و `balance_after` بالجدول الحركات دون قفل متزامن حقيقي، مما يعرض أرصدة التحويلات للتلف (Race Conditions).
* **البند المخالف**: *"جميع العمليات تتم داخل Database Transaction وبقفل حصري للأرصدة"*.
* **الحل المقترح**:
  تعديل دالة `transferTo` لتستخدم `lockForUpdate()` عند جلب الخزنة المصدر والهدف لتأمين حركات التحويل المتزامنة كما هو متبع في الإيداع والسحب.

---

### 4) وجود كود ميت وتالف برمجياً (`UserSelfDebtService`)
* **المشكلة البرمجية**:
  * يحتوي الملف [`UserSelfDebtService.php`](file:///d:/Dev/projects/hwnix-bill-api/app/Services/UserSelfDebtService.php) على منطق مالي منفصل وغير مستدعى بأي مكان بالسيستم.
  * الدالة [`createTransaction`](file:///d:/Dev/projects/hwnix-bill-api/app/Services/UserSelfDebtService.php#L114-L136) تقوم بإنشاء سجل في جدول الحركات `transactions` مباشرة دون تطبيق السحب أو الإيداع الفعلي على حساب الخزنة بالجدول، مما يولد حركات وهمية لا يقابلها تغيير حقيقي بالأرصدة.
* **البند المخالف**: *"عدم تكرار المنطق المالي"* و *"منع التكرار نهائياً لإنشاء القيود وتعديل الخزائن"*.
* **الحل المقترح**:
  إزالة ملف `UserSelfDebtService.php` بالكامل وتنظيف الكود الميت كونه خارج نطاق المحرك المالي الموحد.

---

### 5) تخزين الحقل ثنائي التكرار `remaining_amount` بالجدول
* **المشكلة البرمجية**:
  * يحتوي جدول الفواتير وموديل [`Invoice.php`](file:///d:/Dev/projects/hwnix-bill-api/app/Models/Invoice.php#L112) على حقل `remaining_amount` مخزن في قاعدة البيانات ويتم تحديثه برمجياً باليد في دوال التحصيل.
* **البند المخالف**: *"المتبقي: لا يتم تخزين remaining_amount. بل يتم احتسابه دائماً: remaining = total_amount - paid_amount"*.
* **الحل المقترح**:
  * من الناحية الهيكلية، يجب إزالة العمود `remaining_amount` من الجداول مستقبلاً والاعتماد على Accessor محسوب برمجياً بالذاكرة.
  * كحل مرحلي آمن يمنع كسر العمليات الحالية، يجب إبقاء العمود للتوافق البرمجي شريطة أن يتم تحديثه كلياً بصيغة رياضية موحدة ومقفلة داخل المحرك المالي فقط.
