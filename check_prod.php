<?php require "vendor/autoload.php"; $app = require_once "bootstrap/app.php"; $app->make(\Illuminate\Contracts\Console\Kernel::class)->bootstrap(); $variant = \Modules\Inventory\Models\ProductVariant::withoutGlobalScopes()->with("product.company")->where("barcode", "1000000410")->first(); if($variant) { echo json_encode(["product_active" => $variant->product->active, "is_active_in_store" => $variant->product->is_active_in_store, "company_id" => $variant->product->company_id, "company_store_enabled" => $variant->product->company->store_enabled, "variant_status" => $variant->status]); } else { echo "Not found"; }
